% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pgen_cor.R
\name{pgen_cor_update}
\alias{pgen_cor_update}
\title{Extend a PGEN correlation matrix with new variants}
\usage{
pgen_cor_update(
  R_old,
  A,
  snp,
  B = NULL,
  alt_A = NULL,
  alt_B = NULL,
  keep = NULL
)
}
\arguments{
\item{R_old}{Existing square matrix with matching SNP-ID row and column names.}

\item{A}{PGEN path or prefix containing the old SNPs.}

\item{snp}{New SNP IDs or a one-column SNP-ID file.}

\item{B}{PGEN path or prefix containing the new SNPs. Defaults to `A`.}

\item{alt_A, alt_B}{Target ALT vectors as in [pgen_cor()]. When `B` is
omitted, `alt_B` defaults to `alt_A`.}

\item{keep}{Sample-ID file as in [pgen_cor()].}
}
\value{
A numeric correlation matrix ordered by chromosome, position, ID.
}
\description{
`R_old` must use the same sample selection, founder rule, allele directions,
and missing-value rule as [pgen_cor()]. SNPs already in `R_old` are removed
from `snp`. The old/new blocks are assembled first; a single coordinate
permutation is then applied to both axes.
}
